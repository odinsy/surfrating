# WSL CT 2026 — System B (sequential places, th10000_full points)
scoring_system: th10000
scoring_mode: place_based
current_year: 2026
top_n: 100
allowed_years: [2026]

input_paths: []

output:
  translate_columns: false
  columns:
    - Rank
    - Name
    - Total Points

anonymization:
  enabled: false

sorting:
  enabled: true

bonuses:
  decay:
    enabled: false
  participant_factor:
    enabled: false
  sport_rank:
    enabled: false
  participation:
    enabled: false
